[project]
name = "taxbot-ghana"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "requests>=2.31.0",
    "python-dotenv>=1.0.0",
    "fastembed==0.8.0",
    "numpy>=1.26.0",
]

[tool.vercel]
entrypoint = "api.chat:handler"
